tabela = {"arroz": 2, "batata": 3, "carne": 2}

type(tabela)

# ao executar, buscar por: tabela["arroz"] retornará o número associado ao item

# hashmap - forma para chamar os valores da tabela

# tabela não é lista, écomo se fosse lista, é chamada por colchete

# lista é diferente de dicionário

# tupLA É IGUAL À LISTA, MAS NÃO PERMITE ALTERAR OS ELEMNTOS

tupla = {"arroz": 2, "batata": 3, "carne": 2}

# conjunto se escreve com chaves, conjunto é bom para eliminar elementos repetidos, já que conjunto não aceita repetição de elementos
# na lista, posso escolher um elemento pela sua ordem, no conjunto não

conjunto2 = {1, 2, 3}

lista - [1, 2, 3, 4, 5]
i = 0
i+=1
while i < 5:
    print(i)

# não rolou, gravação com problema, não consegui acompanhar
